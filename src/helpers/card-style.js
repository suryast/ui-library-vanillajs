export function styleText(status) {
  //grid-column location
  return status ? `background-color: black; color: #fafafa;` : ``;
}

export function styleBorder(status) {
  //grid-column location
  return status ? `border-bottom: 0.6rem solid white;` : ``;
}
